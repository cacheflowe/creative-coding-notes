void setup()
{
  size(screen.width,screen.height);
  background(0);
  rectMode(CENTER);
}

void draw(){
  // draw shape
  fill(255);
  rect(width/2, height/2, 100, 100);
  // ellipse(width/2, height/2, 100, 100);
}
